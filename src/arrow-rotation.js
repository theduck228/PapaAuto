const arrow = document.querySelector(".choices[data-type*=select-one]:after")
const active = document.querySelector(".choices__list--dropdown.is-active")
element.addEventListener('click', function() {
    arrow.classList.add('rotated');
});